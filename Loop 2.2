#include<iostream>
#include<cmath>
#include<iomanip>
#include<vector>
#include<algorithm>
using namespace std;

int main() {
	int n, k = 1, res = 1;
	cin >> n;
	if (n == 1) cout << n;
	else {
		for (int i = 2; i <= n; i++) {
			res += k * 2 + i;
			k = k * 2 + i;
		}
		cout << res;
	}
}
